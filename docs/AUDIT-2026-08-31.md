# Auditoría técnica — 31 de agosto de 2026

## Resumen ejecutivo

La aplicación productiva está operativa. Vercel no mostró errores de runtime durante los últimos 7 días y los deployments recientes de producción se encuentran en estado `READY`.

La principal deuda detectada no es un fallo visible para el usuario sino **de organización, reconstrucción y trazabilidad**: existen componentes productivos en Supabase que todavía no están versionados en GitHub, numerosos workflows/scripts de parche histórico y un hosting secundario de GitHub Pages que actualmente falla en cada ejecución.

No se recomienda una limpieza destructiva inmediata. El saneamiento debe hacerse por capas, conservando compatibilidad con todas las chapitas ya emitidas.

## Prioridades

| Prioridad | Hallazgo | Riesgo | Acción recomendada |
|---|---|---|---|
| P1 | 3 Edge Functions con lógica productiva viven sólo en Supabase (`patas-lost`, `patas-account-security`, `patas-admin-shortio`) | No poder reconstruir/auditar completamente producción desde GitHub | Versionar exactamente las tres versiones vivas |
| P1 | El esquema vivo de DB supera ampliamente las 2 migraciones existentes | Recuperación incompleta y cambios difíciles de rastrear | Crear baseline del esquema productivo y continuar con migraciones incrementales |
| P1 | `pages.yml` falla actualmente | Señal roja permanente que oculta errores relevantes | Confirmar si GitHub Pages se usa como backup; corregirlo o retirarlo del CI |
| P1 | Varios workflows ejecutan parches y hacen `git push` sobre el código | Historial ruidoso, despliegues extra y riesgo de mutaciones inesperadas | Archivar workflows de un solo uso tras comprobar que sus cambios ya están consolidados |
| P2 | `index.html` y `mi-cuenta/index.html` son monolíticos | Mayor superficie de regresión y difícil diagnóstico | Modularizar en una refactorización separada, con tests de regresión |
| P2 | Supabase Auth tiene Leaked Password Protection desactivado | Menor protección ante contraseñas comprometidas | Activar y probar registro/cambio de contraseña |
| P2 | RPC `find_tag_by_activation_pin` usa `SECURITY DEFINER` | Superficie de ejecución privilegiada | Verificar flujo y encapsular/restringir si puede hacerse sin romper activación |
| P3 | Proyecto Vercel `patas-a-casa-demo` permanece separado y aparentemente antiguo | Confusión operativa | Confirmar uso y archivar/eliminar sólo si ya no tiene función |
| P3 | No hay CSP/HSTS globales estrictos | Endurecimiento web pendiente | Implementar sólo después de reducir scripts/estilos inline y probar compatibilidad |

## Lo que está bien hoy

### Producción

- Proyecto Vercel principal: `patas-a-casa`.
- Dominio productivo actual: `patas-a-casa.vercel.app`.
- Deployments recientes: `READY`.
- Errores de runtime Vercel observados en últimos 7 días: ninguno.
- Los previews generados por la rama de auditoría también quedaron `READY`.

### Base de datos

Las tablas públicas detectadas son:

- `admin_tag_events`
- `app_admin_config`
- `pending_owner_claims`
- `pets`
- `short_links`
- `sightings`
- `tags`

Todas tienen RLS habilitado.

No existen grants directos de tablas para `anon` ni `authenticated`; por lo tanto, la política pública existente sobre `pets` no expone por sí sola actualmente los registros mediante Data API.

### Administración

La clave administrativa no está embebida en `gestion-chapitas.html`. Se introduce en la sesión del navegador y el backend valida la credencial. `service_role` permanece del lado servidor en la Edge Function.

### Compatibilidad QR

`vercel.json` conserva aliases históricos que desembocan en el mismo flujo:

- `/m/:code`
- `/t/:code`
- `/S/:code`

Esto debe considerarse contrato productivo mientras existan chapitas impresas con dichas rutas.

## Edge Functions: estado exacto

Supabase tiene 18 Edge Functions desplegadas. El estado quedó clasificado individualmente en [`EDGE-FUNCTIONS.md`](EDGE-FUNCTIONS.md):

- **5 productivas y versionadas en GitHub**;
- **3 productivas con drift**;
- **10 retiradas deliberadamente**, cuyo deployment actual sólo responde HTTP `410`.

### Productivas y versionadas

1. `patas-api`
2. `patas-account`
3. `patas-register-v2`
4. `patas-admin-tags`
5. `patas-lost-community`

### Productivas pero todavía no versionadas — P1

1. `patas-lost`
2. `patas-account-security`
3. `patas-admin-shortio`

Estas tres contienen lógica real y deben copiarse exactamente desde la versión viva antes de cualquier modificación.

### Retiradas / compatibilidad

1. `patas-demo`
2. `publish-patas-web`
3. `patas-web`
4. `publish-patas-static`
5. `xhtml-test`
6. `patas-admin`
7. `patas-register-v2-test`
8. `patas-short`
9. `patas-email-activation`
10. `patas-tag-scan`

No tienen lógica productiva actual: devuelven `410 Endpoint retirado/deshabilitado` o equivalente. Aun así, no deben borrarse sin revisar referencias y logs, porque el `410` puede ser una compatibilidad deliberada para clientes antiguos.

## GitHub Actions

### Workflow útil y permanente

`hosting-check.yml` funciona como smoke test del hosting Vercel: exige HTTP 200 y comprueba que la página contenga `Patas a Casa`. Este tipo de workflow debe conservarse y ampliarse gradualmente.

### Workflow con error actual

`pages.yml` intenta desplegar todo el repositorio en GitHub Pages. Su última ejecución sobre `main` falló en `Setup Pages`, mientras Vercel permaneció saludable.

Debe existir una sola decisión explícita:

- **si GitHub Pages es backup intencional:** repararlo y documentar su finalidad;
- **si Vercel es la única producción:** quitar el workflow para eliminar una falsa alarma recurrente.

### Workflows históricos de parche

Se detectaron múltiples workflows asociados a cambios concretos (`add-*`, `fix-*`, `apply-*`, etc.). Al menos uno, `add-lost-details.yml`, ejecuta un script Python, modifica archivos productivos, crea un commit y hace `git push`.

Ese patrón no debería ser el mecanismo estándar de mantenimiento. Un workflow permanente debería principalmente **validar** el código, no reescribirlo y publicarlo automáticamente.

La clasificación inicial está documentada en [`.github/WORKFLOWS.md`](../.github/WORKFLOWS.md).

## Arquitectura frontend

La aplicación sigue un modelo estático/monolítico válido para el tamaño inicial del proyecto, pero actualmente concentra mucha lógica en:

- `index.html` (~43 KB)
- `mi-cuenta/index.html` (~81 KB)

Esto dificulta ubicar errores por dominio. La estructura objetivo futura puede separar:

```text
assets/
  css/
    base.css
    public-profile.css
    account.css
  js/
    config.js
    api.js
    auth.js
    activation.js
    public-profile.js
    account.js
```

No realizar esta extracción junto con la limpieza de workflows. Debe ser una refactorización aislada con pruebas visuales y funcionales.

## Orden de saneamiento recomendado

### Fase 1 — documentación y fuentes de verdad

Hecho en esta rama:
- README profesional;
- mapa de arquitectura;
- inventario de Edge Functions;
- modelo de seguridad;
- procedimiento de operación;
- clasificación de workflows;
- esta auditoría.

### Fase 2 — infraestructura reproducible

- versionar `patas-lost`;
- versionar `patas-account-security`;
- versionar `patas-admin-shortio`;
- conservar inventario de stubs 410;
- crear baseline de esquema/migraciones.

### Fase 3 — CI limpio

- decidir GitHub Pages;
- conservar smoke tests;
- archivar/retirar automatizaciones de un solo uso;
- impedir que workflows históricos modifiquen `main` accidentalmente.

### Fase 4 — frontend mantenible

- separar configuración/API/UI en módulos;
- mantener las URLs públicas intactas;
- agregar pruebas mínimas para perfil, activación, login y cuenta.

### Fase 5 — hardening

- leaked password protection;
- revisar RPC privilegiados;
- CSP/HSTS cuando el frontend lo permita.

## Criterio de seguridad para la limpieza

Nada se elimina hasta que se cumplan conjuntamente:

1. no tiene referencias activas;
2. no muestra uso relevante en logs;
3. existe copia versionada o historial recuperable;
4. preview funciona sin él;
5. las chapitas existentes siguen resolviendo sus URLs y perfiles.

## Conclusión

El sistema no necesita una reescritura. Necesita convertir el estado actual —que funciona— en una plataforma **reproducible, observable y predecible**. Después de clasificar las funciones vivas, el drift productivo real quedó acotado y es abordable: tres Edge Functions, el baseline de base de datos y la limpieza controlada de CI.