name: Volver flujo PIN 20260830

on:
  push:
    paths:
      - '.github/scripts/add_qr_scanner_20260830.py'
      - '.github/workflows/add-qr-scanner-20260830.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Aplicar rollback a PIN
        run: python .github/scripts/add_qr_scanner_20260830.py
      - name: Verificar flujo PIN
        run: |
          grep -q 'function openPinClaim' mi-cuenta/index.html
          grep -q 'function openClaim(){openPinClaim()}' mi-cuenta/index.html
          node --check <(python - <<'PY'
          import re
          h=open('mi-cuenta/index.html',encoding='utf-8').read()
          s='\n'.join(re.findall(r'<script(?![^>]*\\bsrc=)[^>]*>(.*?)</script>',h,re.S|re.I))
          print(s)
          PY
          )
      - name: Commit changes
        run: |
          if git diff --quiet -- mi-cuenta/index.html; then
            echo 'No changes needed.'
            exit 0
          fi
          git config user.name 'patas-a-casa-bot'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add mi-cuenta/index.html
          git commit -m 'Volver Agregar chapita al PIN'
          git push
